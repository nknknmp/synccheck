# 作業ログ 2026-09-19 〜 09-20

この2日で何が起きて、何を直したかの記録。
細かい変更は `CHANGELOG.md`、今の作りは `引継ぎノート.md` にある。
ここは**なぜそうなったか**と**踏んだ落とし穴**をまとめたもの。

戻したくなったら `git checkout v13-working`。

---

## 出発点

「SyncCheck はウェブ上で同期するアプリ？」という質問から始まった。
実際は「2カメのズレを測る道具」で、ローカルでしか動かなかった。

## やったこと（大きい順）

### 1. 出先で使えるようにした

- `serve.py --lan` を足した（同じ Wi-Fi の端末から見る用）
- **SyncCheck だけを public リポジトリに切り出して GitHub Pages で公開**
  （`dev-toolbox` は private のまま。private の Pages は有料のため）
- Service Worker でオフライン対応。一度開けば圏外でも動く

  → https://nknknmp.github.io/synccheck/

### 2. 測定値の符号が逆だったのを直した ★重要

11分の2カメ素材で **+6.90秒** と出た。正解は **-3.50秒**。

`crossCorrelate` が `a[i]` と `b[i+lag]` の一致を「b が遅れている」と
解釈していたが、正しくは「**b が先行している**」。符号を反転して返す
ように直した。

**27件のテストが通っていたのに見つからなかった。**
テスト側も同じ誤りを犯していて、打ち消し合っていたため
（`delay_envelope(a,+D)` は「D秒遅れて始まった b」を作るので
正しい期待値は `-D` なのに `+D` と書いていた）。

再発防止に `test/sign_test.py` を追加。実ファイルから既知の秒数だけ
ずらして切り出すので、実装とテストが同じ勘違いをする構図にならない。

### 3. 作りを根本から変えた（A側/B側 → 1本の軸）

「4本や6本のときに、いちばん早い動画から見てどこに置けばいいか
知りたい」という要望。組ごとに結果を出す作りでは読み取れなかった。

**音で総当たりして、本当に重なる組を見つけてから並べる**方式に。
撮影時刻は位置の計算に使わない（順番を決めるだけ）。

### 4. 表示を「置く位置のタイムコード」にした

「-105フレームと言われても、置くときに計算し直すことになる」
という指摘。いちばん早い1本を `00:00:00:00` として、
それぞれの置き場所をタイムコードで出すようにした。

秒の小数（3.50秒）が「3秒50フレーム」と誤読されていたのも
ここで解消（正しくは 105フレーム = `00:00:03:15`）。

### 5. 4GB 超の素材に対応

`916質疑応答.mov`（4.2GB）でエラー。`file.arrayBuffer()` が
ファイル全体をメモリに載せようとしていた（ffmpeg.wasm は32bit・上限4GB）。

`WORKERFS` でマウントする方式に変更。コピーせずに読む。

### 6. その他

- フレーム数・タイムコード表示、fps の自動検出
- fcpxml の書き出し（オプション・既定オフ・空トラックを作らない）
- 25fps の動作確認
- 診断表示（各行の「内訳」）

---

## 踏んだ落とし穴（同じことを繰り返さないために）

### テストが通っても画面が動かないことがある

作り替えのときに `setBar()` を関数ごと消してしまい、
ボタンを押しても最初の一歩で例外が出て止まっていた。
例外は Promise の中で握られるので**画面には何も出ない**。
利用者からは「押しても無反応」としか見えなかった。

Python のテスト6種は全部通っていた。どれも計算の検算で、
**画面を通していなかった**ため。
→ `test/e2e_check.html` を追加。必ずこれも開くこと。

### 直したものが端末に届かないことがある

Service Worker がキャッシュ優先だったため、符号バグを直して
公開しても iPad には古い版が出続けた。VERSION を上げても、
**その判断をするのが古い Service Worker 自身**なので入れ替わらない。

→ ネットワーク優先に変更（ffmpeg 本体31MB だけキャッシュ優先）。
  画面に版数を表示して、目で確かめられるようにした。

### 移植でコードだけ来て、判断の理由が来なかった

撮影時刻が録画開始か終了かの議論を、SemiCut で一度やって
実ファイルで確定させていたのに、**同じ調査をやり直す羽目になった**。

SyncCheck は SemiCut から同期計算を移植したが、
「なぜそうなっているか」は移植されなかった。
→ 判断の根拠をコードのコメントとノートに書いた。

### 音が「抜けているように見える」ので気づけない

4GB 対策で「先頭＋末尾の moov を切り貼り」する方式を試した。
mp4 では動いたが mov で壊れた。音声は取り出せるので
一見動いているように見えるが、中身が違う。

    同じ603.5秒を抜いても
      フル読み → 相関 +2.20秒（正解）
      切り貼り → 相関 +398.40秒（でたらめ）

**中身が正しいかは相関で確かめないと分からない。**

### 辻褄合わせで回帰を入れた

上の切り貼り方式のまま「読める秒数をそろえる」処理を足したら、
6本の素材で重なりを検出できなくなった（長い素材の後半で重なる
組を見失う）。WORKERFS にしたら根本から不要になり削除。

**おかしいとき、対症療法を重ねると別のものを壊す。**

---

## 素材について分かったこと

### 撮影時刻の意味は端末で違う

| 端末 | 拡張子 | creation_time |
|---|---|---|
| iPad / iPhone | `.mov` | **録画開始** |
| Android | `.mp4` | **録画終了** |

差が動画の長さぶん出る（実素材で11分1秒）。
ただし**秒単位までしか記録されず端末の時計もずれる**ので、
編集に要る1フレーム（0.033秒）の精度には原理的に届かない。
→ 位置は必ず音で決める。

### ファイル名と中身が対応しないことがある

`916烏口突起実技2.mp4` の中身は**実技1の mov と重なっていた**
（音で7窓すべて一致）。名前で組にすると外す。

### 1カメだけの場面が混ざる

すべてのクリップにペアがあるとは限らない。
**重ならないクリップがあるのは正常**。

### 音量差は問題にならない

「カメラ位置が違うと片方だけ大きく録れる」という懸念があったが、
相関の前に正規化するので絶対音量は消える。
実素材でも2本の平均音量が10.2dB違っていたが正しく測れた。
音量1/10・4倍・こもり・残響・ノイズ混入でも答えは変わらない
（`test/volume_test.py`）。

---

## 未確認のまま残っていること

- **iPad 実機での 4GB 級素材**（Safari のメモリ上限は PC より厳しい）
- **fcpxml が編集ソフトで開けるか**（このPCに編集ソフトが無い）
- 画面を白くする要望（保留と言われたまま）
