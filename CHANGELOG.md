# 変更履歴（SyncCheck）

新しいものが上。日付は YYYY-MM-DD。

---

## 2026-09-19（その12）

重ならないクリップを**隙間なく隣接**させるようにした。

### なぜ

「タイムライン上で繋げて表示したいから、実技1が終わったすぐ後の
フレーム数を表示してほしい」という指摘。

それまでは島と島のあいだに **2秒の隙間**を空けていた。これは私が
根拠なく決めた値で、編集のときに手で詰める手間になるだけだった。

### 正しい公式

    次のクリップの開始 = 前の開始フレーム + floor(長さ × fps)

ポイントは2つ:

1. **+1 は要らない**
   フレーム番号は 0 始まり。長さ N フレームのクリップは 0〜N-1 を
   占めるので、次のクリップの先頭はちょうど N。

2. **長さは切り捨て**（round でも ceil でもない）
   実素材 `916烏口突起実技1.mov` は 847.445秒 = 25423.35フレーム。
   切り上げ/四捨五入だと、存在しないフレームぶん先へ進んでしまう。

あわせて、**計算はフレーム単位で積む**。秒のまま足して最後に
フレームへ直すと、端数の丸めで1〜2フレームずれる（実際にずれた）。

### 結果（実素材6本）

    k1.mov   00:00:00:00   基準
    k1.mp4   00:01:08:06   音で測定
    k2.mp4   00:06:58:04   音で測定
    k3.mov   00:14:07:13   単独   ← 指定された値と一致
    k3.mp4   00:14:17:12   音で測定
    k2.mov   00:17:09:06   単独

`k3.mov` が `00:14:07:13`（25423f）。`k2.mov` も `k3.mov` の終端から
隙間なく続いている。

### 確認

- 実素材6本で `k3.mov = 00:14:07:13` を確認（指定値と完全一致）
- `test/lineup_test.py` に隣接の検算を追加
  （847.445秒のクリップの次が 25423フレームから始まること）
- テスト6種すべて通過、通しテスト PASS
- **実機（iPad）では未確認**

---

## 2026-09-19（その11）★音で重なりを見つける方式に変えた

実素材6本（`９１６烏口突起`）で「ことごとくバラバラ」という報告。
作りを根本から変えて解決した。

### 何が起きていたか

撮影時刻を信じて「どれとどれが重なるか」を決めていた。この素材では
それが通用しない:

- `実技2.mp4` の**中身は実技1の mov と重なる**（名前と中身が違う）
- `実技2.mov` は**どれとも重ならない**（1カメだけの場面）
- iPad(mov)=録画開始 / Android(mp4)=録画終了 で時刻の意味が違う

撮影時刻から組を決めると、無関係なクリップを突き合わせて滅茶苦茶になる。

### どう変えたか

**音で総当たりして、本当に重なる組を見つけてから並べる。**

1. 全ファイルの波形を1回ずつ作る（音声だけ・8kHz）
2. 全組み合わせを照合（6本なら15組。波形は手元にあるので計算だけ）
3. 候補位置で**窓ごとに測り直し、全窓が揃うか**で重なりを判定
4. 確かな組から順につないで位置を決める
5. つながらなかったものは「単独」として後ろへ

撮影時刻は「どのあたりを探すか」の当たりにだけ使う。

### 判定はスコアではなく「窓の一致」で見る

総当たりの実測で分かったこと: **相関スコアの大小では線を引けない**。
重なっていない組でも 0.23 は出て、重なっている組が 0.33 のこともある。

決め手は「候補位置で切り出して窓ごとに測り直したとき、どの窓も
同じ答えになるか」。`sync.js` の `verifyOverlap()` がそれをやる。

### 島が複数できる

1カメの場面が混ざるので、全部が1つにつながるとは限らない。
実素材では「実技1の島」と「実技3の島」に分かれた。
島ごとに起点を作り、島どうしは撮影時刻の順に積む。

（途中、2つ目の島を捨てるバグを入れていた。「あとで処理する」と
コメントに書いて処理を書いていなかった。実技3のペアが
「重ならない」と出ていた原因）

### 結果（実素材6本・所要4秒）

    k1.mov   00:00:00:00   基準
    k1.mp4   00:01:08:06   音で測定（窓 4/5）
    k2.mp4   00:06:58:04   音で測定（窓 7/7）
    k3.mov   00:14:09:14   単独
    k3.mp4   00:14:19:12   音で測定（窓 2/2）
    k2.mov   00:17:13:07   単独

事前に音だけで求めた値（+68.19 / +418.14 / +9.95秒）と照合して、
**3組とも誤差0フレーム**。

### 直したこと

- `SCAN_LEN_SEC` で音声を600秒に切っていて、それ以降で重なる組を
  見つけられなかった。全長を読むようにした
- 診断表示が古いデータ形式のままで、表示が落ちていた
- 「単独」を「撮影時刻のまま」と出していた。1カメの場面は
  重ならないのが正常なので、異常に見えない言い方に変えた

### 確認

- 実素材6本を画面経由で通し、3組すべて誤差0フレーム
- `test/lineup_test.py` を新方式向けに書き直し
  （島2つ+単独1本、島の後からの結合、重なり不足の除外）
- 通しテスト（`test/e2e_check.html`）PASS
- テスト6種すべて通過
- **実機（iPad）では未確認**

---

## 2026-09-19（その10）

「別の動画でもずれる」という報告を受けて、原因を切り分けるための
**診断表示**を足した。あわせて NaN のバグを1つ直した。

### 実素材（916まとめ）では正解が出ている

アプリと同じ経路をブラウザで通して追跡した結果:

    guessStarts    = 0.000 , 1.700
    aOffsetSec=1.700 bOffsetSec=0.000 overlap=659.3秒
    measuredSec    = -5.200  （スコア 0.501）
    位置計算       = 0 + 1.700 + (-5.200) = -3.500
    → mov=3.500秒 / mp4=0秒   ← 正解と一致

つまりこの2本では計算は合っている。**ずれるのは別の条件**。
推測で直さず、画面で切り分けられるようにした。

### 足した診断表示

結果の各行に「内訳」を出す（折りたたみ）。

    基準にした相手: A_noise.mp4
    撮影時刻から見た差: 0.00秒
    音で測った補正: -3.13秒
    = 置き位置 0.00秒
    重なり: 70秒 / 相関スコア: 0.918
    窓ごとの測定値: -3.13 / -3.13 / -3.13（ばらつき 0.00秒）

これで、ずれたときに

  - **撮影時刻の推定**が悪いのか
  - **音の測定**が悪いのか（窓がばらついていないか）
  - そもそも**誰を基準に測ったか**

が分かる。窓の値が揃っていれば測定は信用でき、ばらついていれば
音が似ている区間で誤検出している。

### 直したバグ

`lineup.js` が `measurePair` に `startSec` を渡しておらず、
`timeGapSec` と `totalOffsetSec` が **NaN** になっていた。
lineup では `measuredSec` しか使わないので表には出ていなかったが、
壊れた値を返すのは良くないので直した。

### 端末の判別について（調べた結果）

実素材のメタデータを見ると、端末は確実に分かる:

    mov: com.apple.quicktime.model=iPad / make=Apple
    mp4: com.android.version=13

「iPad=録画開始 / Android=録画終了」という読みもこの素材では合う。
ただし**それだけでは足りない**:

    その解釈から出る値 : +1.714秒
    音で測った本当の答え: -3.500秒
    ずれ               : 5.2秒 = 156フレーム

撮影時刻は**秒単位までしか記録されていない**（ミリ秒は全部ゼロ）うえ、
2台の端末の時計そのものが数秒ずれている。編集に要る精度は
1フレーム = 0.033秒 なので、原理的に届かない。

撮影時刻は「どのあたりを探すか」、音は「1フレーム単位の位置」。
役割が違う。

---

## 2026-09-19（その9）★「重なっていない」と誤判定する不具合を修正

916まとめ の2本で「重なっていない」と出た。実際はほぼ全域が重なっている。

### 原因

撮影時刻が「録画開始」なのか「録画終了」なのか分からないのに、
`lineup.js` では**どちらも開始だと決めつけて**いた。

    mov: 08:32:36 / 661秒  ← 録画開始を記録
    mp4: 08:43:37 / 659秒  ← 録画終了を記録

両方を開始とみなすと mp4 が 11分1秒後に始まることになり、
**重なりが 0.3秒**（しきい値10秒未満）→「重なっていない」で
切り捨てていた。

旧 `timeline.js` の `resolveRecordingTimes` には
この罠への対処（開始・終了の2案を試す）が入っていたのに、
`lineup.js` に作り替えるとき**持ち込むのを忘れていた**。

### どう直したか

`guessStarts()` で、時刻ごとに

    [埋め込みが開始とみた場合, 終了とみた場合]

の2案を持ち、**全体の重なりが最大になる組み合わせ**を選ぶ。
2台のカメラは同じ時間帯を撮っているはず、という事実を使う。
1本ずつ案を切り替えて重なりが増えるなら採用、を収束するまで繰り返す。

### 結果

    修正前: mov 0.0秒 / mp4 661.0秒 → 重なり   0.3秒 → 測れない
    修正後: mov 0.0秒 / mp4   1.7秒 → 重なり 659.3秒 → 測れる

mp4 の時刻を正しく「録画終了」と解釈している。

### 確認

- `test/lineup_test.py` に実素材の撮影時刻での検算を追加
  （素直に並べると 0.3秒 で切り捨てられることも明記）
- ブラウザでも同じ推定になることを確認（mov 0.0 / mp4 1.7 / 重なり 659.3秒）
- 通しテスト（test/e2e_check.html）も PASS。
  正解 3.130秒 に対し 94フレーム / 00:00:03:04
- テスト6種すべて通過

---

## 2026-09-19（その8）★押しても動かない不具合を修正

v6 で「置く位置を出す」を押しても**何も起きない**状態になっていた。

### 原因

作り替えのときに `setBar()`（進捗バーを動かす関数）を
**関数ごと消してしまっていた**。

ボタンを押すと最初の `setBar(0)` で
`setBar is not defined` が投げられ、その時点で止まる。
例外は Promise の中で握られるので画面には何も出ず、
ログも進捗バーも動かない。利用者からは
「押しても無反応。時間がかかっているのかすら分からない」
としか見えなかった。

`showError()` も同時に消えていた（失敗時に何も出せない状態）。
両方とも復元した。

### なぜテストで見つからなかったか

Python のテスト6種はすべて通っていた。それらは計算の検算であって、
**画面を通していない**ため。

`test/e2e_check.html` を追加した。iframe で本体を開き、
実ファイルを input に流し込んでボタンを押す。利用者と同じ経路を
通るので、この種の抜けを拾える。ブラウザで開いて使う。

### あわせて直したこと

基準の1本に「撮影時刻」と出ていた。基準は測る対象ではなく
測定の土台なので、「撮影時刻のまま＝疑わしい」と読めてしまう。
**基準 / 音で測定 / 撮影時刻のまま** の3つを区別した。

### 確認

- 画面を通して実ファイル2本で測定。正解 3.130秒 に対し
  **94 フレーム / 00:00:03:04** を表示（計算上の 94 と一致）
- テスト6種すべて通過

---

## 2026-09-19（その7）★作りを変えた

**A側/B側をやめて、全部を1本のタイムラインに並べる**形にした。

### なぜ

「4本のときも6本のときもある。それらを**いちばん早い動画から見て、
どこにクリップを置けばいいか**を知るためのアプリにしてほしい」
という要望。

組ごとに結果を出す作りだと、本数が増えるほど組の数が増えて、
「結局どこに置くのか」が読み取れなかった。

### 何を変えたか

- **放り込み口を1つに**した。A側/B側に分けなくてよい
- 結果は**1本のリスト**。いちばん早いものが 00:00:00:00

      916まとめ.mp4   00:00:00:00       0 フレーム
      916まとめ.mov   00:00:03:15     105 フレーム

- `src/lineup.js` を新設。1本目を基準に順につないでいく。
  測る回数は「本数 - 1」（総当たりはしない）
- 相手は**すでに置いた中でいちばん重なりが長いもの**を選ぶ。
  重なりが長いほど答えが安定し、誤差の積み上がりも減る
- 測れなかったものは撮影時刻のまま置き、画面にその旨を出す
- 書き出しも並び順のテキスト / CSV に変更

fcpxml と JSON の書き出しは、旧形式が前提なのでいったん外した。
必要になったら新しい形で作り直す。

### 音量差について（実測で確かめた）

「カメラの位置が違うと、A では小さく B では大きく録れる。
それでずれるのでは」という懸念があった。**ずれない。**

相関を取る前に normalize() で平均を引いて標準偏差で割るので、
音量の絶対値は消える。残るのは変化のタイミングだけ。

実素材（916まとめ）の2本も平均音量が 10.2dB（振幅で約3.2倍）
違っていたが、正しく測れていた。

`test/volume_test.py` を追加。音量 1/10・4倍、こもり、残響、
ノイズ混入まで試して、答えが変わらないことを確認している
（スコアは 1.05 → 0.97 に下がるが、値は +3.00秒 のまま）。

### 撮影時刻は当てにしない

実素材で mov=録画開始 / mp4=録画終了 を記録していて、
11分1秒ずれていた。そのまま信じると 5.2秒ずれる。
撮影時刻は「どのあたりを探すか」の目安にだけ使う。

### 直したこと

`src/audio.js` と `src/export.js` に壊れた文字列リテラルが
入っていた（`
` が生の改行になり、正規表現と join が壊れていた）。
ブラウザのコンソールに出た SyntaxError で気づいて直した。
ローカルの Python テストでは見つからない種類の不具合。

### 確認

- `test/lineup_test.py` を追加。位置の組み立てを検算
  （2本・4本・6本、順番の入れ替わり、重ならない1本が混ざる場合）
- ブラウザで実素材と同じ条件を通して確認。
  `916まとめ.mp4 → 00:00:00:00` / `916まとめ.mov → 00:00:03:15`
- テスト sync 27 / fcpxml 42 / sign / frame / volume / lineup すべて通過
- **実機（iPad）では未確認**

---

## 2026-09-19（その6）

**タイムラインに置く位置**をタイムコードで出すようにした。

### なぜ

「-105 フレームずらす」と言われても、タイムラインに置くときは
そこから位置を計算し直すことになる、という指摘。最初から
置き場所を出せばその手間が要らない。

あわせて、秒の小数表記が誤解を生んでいたのも分かった。
`3.50秒` を「3秒50フレーム」と読まれていた。正しくは
105フレームで、タイムコードなら `00:00:03:15`。
紛らわしいので、置き場所はタイムコードで見せることにした。

    3.50秒  = 小数の秒 = 105フレーム
    3:15    = 3秒15フレーム = 105フレーム   ← 同じ位置

### 何を変えたか

- 結果カードに**置き場所の表**を出す。先に始まっているほうを
  00:00:00:00 に置き、もう片方をずらす。こうすると負の位置が
  出ないので、そのまま打ち込める

      916まとめ.mov   00:00:03:15   105 フレーム
      916まとめ.mp4   00:00:00:00     0 フレーム

- 「ずらす量」は補足に降格（括弧内に残す）
- テキスト書き出しと CSV にも置き場所を追加。
  CSV は A/B それぞれの TC とフレーム番号を列に持つ
- テキストの「読み方」を書き直した。3:15 と 3.15秒 の違いを明記

### ドロップフレームは使わない

29.97 の放送用には `;` 区切りのドロップフレーム表記があるが、
採らない。このアプリが出すのは「素材を置く位置」であって時刻では
ないため。番号を飛ばすと、フレーム数から計算した位置と表記がずれる。

### 確認

- `test/frame_test.py` にタイムコードと置き場所の検算を追加
  （105フレーム → 00:00:03:15、先発側が必ず 0、負の位置を出さない）
- ブラウザでも同値を確認
- 実素材の模擬データで書き出しを確認。
  テキスト・CSV とも A側 00:00:03:15 / B側 00:00:00:00
- テスト sync 27 / fcpxml 42 / sign 4 / frame すべて通過
- **実機（iPad）では未確認**

---

## 2026-09-19（その5）

ズレを**フレーム数で表示**するようにした。

### なぜ

秒で出しても編集ソフトで合わせづらい、という指摘。編集ソフトは
フレーム単位でしか動かせないので、秒より frame のほうが現場で使える。

### 何を変えたか

- 結果の大きな数字を**フレーム数**にした（秒は括弧で併記）
  例: `-105 フレーム`（`30fps / -3.50秒`）
- フレームレートの選択を**測定欄の下へ移動**した。
  以前は fcpxml 書き出しの中にあり、結果表示には使われていなかった。
  選択は1か所にまとめ、fcpxml もそれを使う
- **素材から自動で読む**ようにした（`readMeta` が ffmpeg のログの
  `30 fps` を拾う）。A側とB側で違うときは A側 を採り、画面に断る。
  黙って決めると1フレームずれても気づけないため
- フレームレートを変えたら、**測り直さずに表示だけ作り直す**
  （測定値は秒で持っているので換算をやり直すだけでよい）
- 丸めで 5ミリ秒以上の端数が出るときは画面に出す
- テキスト書き出しにもフレーム数を入れた

### 29.97 の扱い

`realFps()` で 30000/1001 に直してから計算する。

ただし**実際に答えが割れるのは約20000秒（5時間半）を超えてから**で、
同期のズレは普通 数秒なので現場では差が出ない（実測して確かめた）。
それでも分数を使うのは、式として正しいほうを選んでおくため。
最初この件を「長い素材でずれる」と書いていたが、1時間では一致する
ので誤り。実測に合わせて直した。

### 確認

- `test/frame_test.py` を追加。換算・符号・端数・29.97 の検算
- ブラウザでも同じ値が出ることを確認
  （`-3.50秒 → -105 フレーム`、`+3.50秒 → +105 フレーム`）
- テスト sync 27 / fcpxml 42 / sign 4 / frame すべて通過
- **実機（iPad）では未確認**

---

## 2026-09-19（その4）★直したものが端末に届かない問題

符号を直して公開したのに、iPad では直前と**まったく同じ +6.90秒**が
出続けた。原因は Service Worker のキャッシュ戦略。

### 何が起きていたか

`sw.js` の fetch が**キャッシュ優先**だった。

    手元にあれば、ネットに繋がっていてもそれを返す

この作りだと、古い Service Worker が自分の持っている古い
`src/sync.js` を返し続ける。VERSION を上げても、**その判断をするのが
古い Service Worker 自身**なので入れ替わらない。
利用者の側から直す手段が事実上無くなる（キャッシュ全消ししか無い）。

オフライン対応と引き換えに、**バグを配布したら取り消せない**
状態を作っていた。

### どう直したか

ネットワーク優先に変えた。ただし ffmpeg 本体（31MB）だけは
キャッシュ優先のまま残す。

- `.wasm` … キャッシュ優先。中身は変わらないし、毎回 31MB を
  取りに行くと出先で破綻する。VERSION を上げたときだけ入れ替わる
- それ以外 … ネット優先。**3秒で見切って**キャッシュに落とす。
  細い回線で待たされ続けないため。圏外での起動はこれまでどおり

あわせて:

- 画面の見出しに**版数（v3）を表示**した。古いものが動いていないか
  目で見て分かるようにする
- 起動のたびに `registration.update()` を呼ぶ。新しい版が出たら
  「開き直すと切り替わります」と出す

### 確認

- ブラウザ（Chrome）で実素材を使った符号検算 … **PASS**
  A を5秒目・B を8秒目から切り出し（B が3秒先行）→ 測定 +3.00秒
- 既存テスト sync 27件 / fcpxml 42件 / sign 4件 すべて通過

---

## 2026-09-19（その3）★重要な修正

**測定値の符号が逆だった。** 実素材で発覚し、原因を特定して直した。

### 症状

11分の2カメ素材（mov / mp4）で **+6.90秒** と表示された。
正解は **-3.50秒**。符号も大きさも違う。

### 原因

`src/sync.js` の `crossCorrelate` が返す符号が逆だった。

    a[i] と b[i+lag] が一致した

これは「a の i 番目と同じ音が b の i+lag 番目にある」ということで、
**b のほうが lag だけ先に始まっている**（b が先行）。
コメントは「b が lag だけ遅れている」と書いてあり、逆だった。

この関数は「b の遅れ」を返す約束なので、符号を反転して返すよう直した。

### なぜ27件のテストで見つからなかったか

**テストが同じ誤りを2か所で犯して、打ち消し合っていた。**

    delay_envelope(a, +D) は「D秒 遅れて始まった b」を作る
      → 合わせるには b を前へ戻す。正しい期待値は -D
      → なのに期待値を +D と書いていた

素材の作り方と期待値の両方が同じ向きに間違っていたので、
符号が逆のままでも通ってしまう。同じ思い違いで書いた2つを
突き合わせても、誤りは出てこない。

### どう確かめたか（推測ではなく実測）

1. ネイティブ ffmpeg で実素材の全域相関 → **-3.50秒**。
   60秒窓5つすべて一致、ばらつき 0.01秒
2. アプリの処理を再現 → 修正前 **+6.88秒**（報告の +6.90 と一致）
3. 修正後に再現 → **-3.48秒**（正解との差 0.02秒）
4. 決め手: 同じ1本から 100秒目と 105秒目を切り出して測る。
   真値は ffmpeg の -ss が決めるので思い込みが入らない。
   反転ありが +5.00 を返し、これが正しい

### 再発防止

`test/sign_test.py` を追加した。**実ファイルから既知の秒数だけ
ずらして切り出し**、符号だけを検算する。合成素材を使わないので、
今回のような「テストと実装が同じ勘違いをする」構図にならない。
修正前のコードなら必ず落ちる。

`test/sync_test.py` の `cross_correlate` と期待値も直した
（期待値は `shift_to_align()` を通して書くようにした）。

### 影響

**これまでの測定値はすべて符号が逆だった可能性がある。**
過去に書き出した結果を使っているなら測り直すこと。

`sw.js` の VERSION を v2 に上げた（端末の古いキャッシュを捨てるため）。

---

## 2026-09-19（その2）

出先で PC 無しに使えるよう、単独の public リポジトリに切り出して
GitHub Pages に公開した。あわせてオフライン対応を入れた。

### なぜそうしたか

このアプリは**出先で使うのが前提**だった（家の PC を立てる前提の
`--lan` では用を成さない）。サーバー側の処理が無く、外部通信のコードも
ゼロなので、静的ホスティングにそのまま置ける。

`dev-toolbox` は private のままにしたいが、private の Pages は有料。
SyncCheck だけを切り出せば、toolbox の他のプロジェクトは private の
まま、このアプリだけを公開できる。`git subtree split` を使ったので
SyncCheck の3コミットの履歴はそのまま残っている。

公開して困るものが無いことは確認した: 外部通信 0 件、API キー無し。
**動画は今までどおり端末から出ない**ので、コードが読めることと
素材の安全性は別の話。

### 何を足したか

- `sw.js` … Service Worker。初回に一式（wasm 約31MB 込み）を端末に
  取り込み、2回目からはネットを見ない。圏外でも起動する。
  `addAll` ではなく1件ずつ入れている。31MB が途中で切れただけで
  全部捨てられると、細い回線では永久に揃わないため。
- `manifest.webmanifest` + `icon.svg` … ホーム画面に追加するとアプリの
  ように開ける。アイコンは「同じ波形が横にずれている」図にした。
- `index.html` に登録コードと iPad 用の meta を追加。

### 直したこと

Service Worker の取り込み一覧に `manifest.webmanifest` と `icon.svg` を
入れ忘れていた。オフライン確認のテストで「必須12件中2件欠け」と出て
気づいた。オフライン時にホーム画面アイコンが出ない不具合になる。

### 確認したこと

- `test/sw_check.html` … 登録 → activate → 22件キャッシュ、
  wasm が 32,129,114 bytes で欠けなく入ることを Chrome で確認。
- `test/offline_check.html` … オフライン起動に要る12件が
  すべてキャッシュにあることを確認（欠け0）。
- 既存テスト `sync_test.py` 27件 / `fcpxml_test.py` 42件、失敗0。
- **iPad 実機では未確認**。検証は PC の Chrome ヘッドレス。
  Safari の Service Worker とストレージ上限の挙動は実機で見る必要がある。

---

## 2026-09-19

iPad から実機確認できるよう、`serve.py` に `--lan` を足した。

### 何を変えたか

- `python serve.py --lan` で `0.0.0.0` を待ち受けるようにした。
  起動時に iPad から打つ URL（`http://192.168.x.x:8770/`）を表示する。
- IP は UDP ソケットの「繋いだふり」で OS に聞く（`lan_ip()`）。
  `ipconfig` を読むと Hyper-V の仮想スイッチ（192.168.80.1）を
  拾ってしまい、iPad からは繋がらない番号を案内することになる。
- `使い方.txt` の「ネット越しに使う」を書き直した。

### なぜそうしたか

既定は `127.0.0.1` のままにした。このサーバーは SyncCheck フォルダの
中身をそのまま返すので、必要なときだけ外に出す。

GitHub Pages 案は今は使えないと分かったので、その旨を `使い方.txt` に
残した。`dev-toolbox` が private で、private の Pages は有料プランが要る。
toolbox 全体を public にはできないため。

### 確認したこと

- `--lan` で起動し、`http://192.168.0.3:8770/` から
  index.html / `vendor/core/ffmpeg-core.wasm`（32MB, application/wasm）/
  `src/audio.js`（text/javascript）がすべて HTTP 200 で返ることを確認。
- **iPad の実機では未確認**（この作業時点で iPad を繋いでいない）。
  ffmpeg は単スレッド版なので `SharedArrayBuffer` は要らず、
  HTTPS でなくても Safari で動く見込み、というところまで。

---

## 2026-09-17（新規作成）

2カメのズレを測るだけのウェブアプリを作った。SemiCut とは別のプログラム。

### なぜ作ったか

SemiCut は「切って・重ねて・書き出す」まで全部やるので、
PC で `python serve.py` を動かす必要がある。
「ズレを測るところだけブラウザでやりたい」という要望があった。

測るのに必要なのは **8kHz モノラルの音量の線だけ**で、映像は1フレームも
要らない。だから動画をアップロードせずブラウザ内で完結できる
（`-vn` で映像を読まない。1時間の動画でも音声だけなら軽い）。

### 何ができるか

- A側N本 / B側M本を放り込むと、撮影時刻から**重なる組を自動で見つける**
- **組ごとに別々にズレを測る**（これが主目的。下記）
- 窓ごとの測定値とドリフト判定を出す
- テキスト / CSV / JSON / fcpxml で書き出す（fcpxml はオプション）

### 「途中で切れて再開する」への対応が主目的

2カメでも録画を止めて再開するため、A側3本・B側3本のような構成になる。
**再開するたびにズレ方が変わる**ので、1組目の値を他の組に当てはめると
全部ずれる（移植元のノートに実測が残っている）。

そのため組ごとに独立して測る設計にした。
組ごとのズレが1秒以上違うときは「1つの値で全体を合わせることはできない」
と警告を出す。

### SemiCut から移植したもの

同期の計算には実測で確かめた判断が入っているので、コメントごと移植した。

| 移植したもの | 何をするか |
|---|---|
| `crossCorrelate` | 相互相関。符号の導出をコメントに残した |
| `verifySync` | 窓ごとに測って**多数決**で決める |
| `buildEnvelope` | 8kHz PCM → 100Hz 音量包絡線 |
| `resolveRecordingTimes` | 更新日時と埋め込み撮影時刻から録画開始を決める |
| `placeOnTimeline` | 複数本を共通の秒数軸に載せる |
| `findOffsetFullRange` | 撮影時刻が壊れているとき音だけで探す |

ファイルは別なので、直しても SemiCut に影響しない。

### 移植元には無かった修正（実測で見つけた）

**重なりが短い lag を見ないようにした**（`MIN_OVERLAP_RATIO = 0.5`）。

相互相関のスコアを重なり長で割って正規化していても、
重なりが数秒まで減ると偶然そこだけ形が合って高いスコアが出る。

  20秒の窓に探索幅15秒を与えたとき、
    正解  +3.13秒（重なり17秒）
    誤答 -13.59秒（重なり 6秒）← こちらのスコアが高かった

「窓の半分以上は重なる lag だけ見る」条件を入れて直した。
あわせて `measure.js` で、全体で1回測ってから
その答え±5秒の範囲で窓ごとに測るようにした
（窓に対して広すぎる探索幅を与えないため）。

移植元は窓を30秒以上に保っていたので、この問題が出ていなかった。

### 検算

実素材が無いので、答えが分かっている素材を ffmpeg で作って確かめた。

- `test/sync_test.py` … 同期の計算 27件（Python に写して照合）
- `test/fcpxml_test.py` … fcpxml の書き出し 42件
- `test/parse_check.html` … 各モジュールが読めるか・export 漏れ
- `test/wasm_check.html` … **ffmpeg.wasm で実ファイルから測れるか**
- `test/ui_demo.html` … 結果画面の見た目

`test/fixtures/` の素材は正解が **+3.130秒**。
ネイティブ ffmpeg で5通りの測り方をして全部一致することを確認した値。

ブラウザの ffmpeg.wasm で測っても **+3.130秒**（差 0.000秒）だった。
アップロードせずに正しく測れることを実ファイルで確認できた。

頭出しのズレも 0.000秒。移植元で concat 経由だと 1.01秒 ずれた件は、
**1本ずつ抜く**（つなぐ処理を持たない）ことで回避している。

### Node.js が無いので Python で検算した

このPCに Node.js が入っていなかった（サブPC側で使っていたらしい）。
`test/sync.test.mjs` は残してあるが動かせないので、
同じ内容を `test/sync_test.py` に写して使っている。

### vendor/ を Git に入れた

GitHub Pages で配信するにはリポジトリ内にファイルが必要なため。
約31MB（SemiCut の 37MB から、使わない日本語フォント 5.5MB を除いた）。

SemiCut は `vendor/` を `.gitignore` で除外しているが、
こちらは方針を変えている。バイナリは一度入れたら更新されないので
リポジトリが膨らみ続けることはない。

親の `toolbox/.gitignore` が `*.mp4` を除外しているので、
`SyncCheck/.gitignore` でテスト素材だけ打ち消している（合計1.6MB）。

### 確かめていないこと

- **fcpxml が Resolve / FCP で開けるか** … 実機未確認（編集ソフトが無い）
  XML として整形式・時間がフレーム単位の分数・位置が負にならないことは確認済み
- **本番素材（数十分〜1時間）で通るか** … 未確認。テストは70秒の合成素材
- **iPad Safari で動くか** … 未確認。SharedArrayBuffer を使わない構成にはしてある
- **GitHub Pages で配信できるか** … Pages の設定をまだしていない
