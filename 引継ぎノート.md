# 引継ぎノート（SyncCheck）

次に開いた人（次回の自分・別のPC）が、説明なしで続きを始めるためのノート。
作業の区切りごとに更新する。新しいものが上。

**このノートは SyncCheck のことだけを書く。**
全体の話は `C:\dev\引継ぎノート.md`、SemiCut の話は
`C:\dev\toolbox\SemiCut\引継ぎノート.md` にある（混ざると読めなくなる）。

---

## ▶ 2026-09-25 このPCに clone した／書き出し名を変えた

### このPCでの置き場所（重要）

    C:\dev\synccheck\        ← ここが作業場所。`nknknmp/synccheck` の clone

**9/24 まで、このリポジトリはこのPCに存在しなかった。**
GitHub（と Pages）にしか無く、9/19〜20 の作業はもう一台のPCでやっていた。
そのPCは故障したため、今後はこのPC 1台で進める。

`toolbox` の外に置いたのは、`dev-toolbox` の中だと別リポジトリの入れ子に
なるため。`C:\dev\toolbox\SyncCheck\`（古い方）はそのまま残してある。

### やったこと

書き出すファイル名を、日時から**基準クリップの名前**に変えた。

    cam1.mp4 が基準  →  cam1_同期済.fcpxml（.txt / .csv も同じ）

`src/export.js` に `outName()` を追加し、`index.html` の3か所の
`saveText()` に通した。使わなくなった `stamp()` は消した。
経緯と選んだ理由は `CHANGELOG.md` の 2026-09-25 の項にある。

テストは `test/outname_test.py`（14件）。既存の7本も通っている。
**ブラウザで開いての確認はしていない**（Python のテストだけ）。
このリポジトリは過去に「テスト全通でも画面が無反応」を踏んでいるので、
次に触るときは下記「触る前に動かすもの」を先に実行すること。

### 次にやること

1. **未 push なら push する。** push するまで Pages（iPad で使う方）には
   反映されない。
2. iPad 実機での確認。下の 2026-09-19 の項に手順がある（最優先のまま）。

---

# ▶ 何があったか知りたいとき

`作業ログ_2026-09-19_20.md` に、この2日の経緯をまとめてある。

- 何をどう変えたか（大きい順）
- **踏んだ落とし穴**（テストが通っても画面が動かない、
  直したものが端末に届かない、音が抜けているように見えて中身が違う等）
- 素材について分かったこと（端末ごとの撮影時刻、名前と中身の食い違い等）

細かい変更履歴は `CHANGELOG.md`。

---

# ▶ おかしくなったら、ここに戻す

**2026-09-20 の `v13-working` が、実素材で動作確認できた状態。**

## 戻し方

いじっておかしくなったら、これで戻る:

    cd <synccheck のフォルダ>
    git stash            # 直しかけを退避（捨ててよいなら不要）
    git checkout v13-working

公開先（GitHub Pages）まで戻したいときは、main をその位置に戻して push:

    git checkout main
    git reset --hard v13-working
    git push --force-with-lease

`--force-with-lease` は、他の人が push していたら止まる安全版。
`--force` は使わないこと。

**一部だけ戻したいとき**（例: lineup.js だけ）:

    git checkout v13-working -- src/lineup.js

## この状態で動くこと（確認済み）

    ９１６烏口突起（6本・所要4秒）
      k1.mov  00:00:00:00  基準
      k1.mp4  00:01:08:06  音で測定（窓4/5）
      k2.mp4  00:06:58:04  音で測定（窓7/7）
      k3.mov  00:14:07:13  単独
      k3.mp4  00:14:17:12  音で測定（窓2/2）
      k2.mov  00:17:09:06  単独

    916質疑応答（4.2GB + 1.6GB・所要35秒）
      qa.mp4 = 66フレーム = 2.20秒（正解 +2.19秒、窓104/105一致）

    25fps の素材 … fps自動検出25 / 2.00秒=50フレーム

**直したあとは、この数字が変わっていないか必ず確かめること。**
特に `k3.mov = 25423フレーム` は何度も壊した箇所。

## 確かめ方

    python serve.py
    → http://localhost:8770/test/e2e_check.html を開く   （画面を通す）

    python test/sync_test.py          27件
    python test/fcpxml_test.py        42件
    python test/sign_test.py          符号
    python test/frame_test.py         フレーム換算・25fps
    python test/volume_test.py        音量差に強いこと
    python test/lineup_test.py        並べ方・島・隣接
    python test/fcpxml_lineup_test.py レーン割り当て

**Python のテストが全部通っても画面が動かないことがある**
（2026-09-19 に setBar() を消して「押しても無反応」になった）。
必ず e2e_check.html も開くこと。

---

# ▶ 触る前に読むもの（これを飛ばすと同じ調査を繰り返す）

## 撮影時刻についての実測（2026-09-19 確定）

**同じ議論を2回やる羽目になったので、ここに書いておく。**
SyncCheck の同期計算は SemiCut からの移植だが、コードだけ来て
「なぜそうなっているか」が来なかったため、撮影時刻が録画開始か
終了かを最初から調べ直すことになった。以下は実ファイルで確定した事実。

### どの端末がどちらを記録するか

| 端末 | 拡張子 | メタデータでの見分け方 | 埋め込み撮影時刻の意味 |
|---|---|---|---|
| iPad | `.mov` | `com.apple.quicktime.model=iPad` / `make=Apple` | **録画開始** |
| iPhone | `.mov` | （下記・タグが無いことがある） | **録画開始** |
| Android | `.mp4` | `com.android.version=13` | **録画終了** |

#### iPhone の実測（2026-09-20）

`Downloads\LowCamera-Video-448159D3-....mov`（3.839秒）:

    全体の creation_time        15:02:21
    映像/音声の creation_time   15:02:22   ← 1秒あと

ファイルを作った時刻より**ストリームの記録開始が後**。
つまりこの時刻は**録画開始**。終了を記録するなら逆になる。
Windows の更新日時（JST 00:02:27 = UTC 15:02:27）が
「開始 + 長さ + 数秒」と整合するのも裏づけ。

**注意**: この素材には `com.apple.quicktime.model` などの
Apple 固有タグが**1つも無かった**（`major_brand=qt` のみ）。
ファイル名から標準カメラではなくサードパーティ製アプリ
（LowCamera）で撮ったものと思われる。アプリによって
メタデータの書き方が変わるので、**タグでの端末判別は当てにしない**。

なお fps は `77/3 ≒ 25.67` という半端な値だった（可変フレームレート
の可能性）。選択肢では最も近い 25 が選ばれる。

実測（`Downloads\916まとめ.{mov,mp4}`）:

| ファイル | 埋め込み | 長さ |
|---|---|---|
| 916まとめ.mov（iPad） | 08:32:36 | 661.3秒 |
| 916まとめ.mp4（Android） | 08:43:37 | 659.3秒 |

差が **11分1秒 ＝ ほぼ動画の長さ**。これが「意味が違う」ことの証拠。

### それでも撮影時刻で位置を決めてはいけない

端末を判別して正しく解釈しても、**音で測った答えとの差が
5.2秒（156フレーム）**あった。理由:

- 埋め込みは**秒単位までしか無い**（ミリ秒は全部ゼロ）
- 2台の端末の**時計自体が数秒ずれる**

編集に要る精度は1フレーム＝0.033秒。**原理的に届かない。**
**撮影時刻は位置の計算に一切使っていない。**

使うのは次の2つだけ:

- 処理する順番を決める（どれから照合するか）
- 音で決められなかったクリップを、どの順に後ろへ並べるか

重なりの探索は `findOffsetFullRange`（全域）で、撮影時刻を渡していない。
だから撮影時刻が録画開始でも終了でも結果は変わらない。

（v9 までは撮影時刻で探す範囲を絞っていた。v10 で音の総当たりに
変えたときに、その絞り込みは無くなっている。古い説明が
コードのコメントに残っていたので 2026-09-20 に直した）

### ファイル名は信用しない

`Downloads\９１６烏口突起\` の素材では、**名前と中身が対応していなかった**。
`実技2.mp4` の中身は `実技1.mov` と重なる（音で7窓すべて一致）。
名前で組にすると外す。だから総当たりで音を照合している。

### 大きいファイル（4GB 超）は WORKERFS でマウントする

`file.arrayBuffer()` はファイル全体をメモリに載せるので、
ffmpeg.wasm（32bit・上限4GB）では 4GB 超が扱えない。
`src/bigfile.js` の `mountInput()` が WORKERFS に切り替える。

実測: `916質疑応答.mov`（4.2GB）が readMeta 254ms で読めた。

**「先頭＋末尾の moov を切り貼りする」方式は捨てた。**
mp4 では動くが mov（pcm_s16le）で壊れる。mdat の中で映像と音声が
細かく交互に並ぶため、サイズ宣言だけ書き換えても moov の索引が
元の全長を指したままになり、誤った位置からサンプルを拾う。

    同じ 603.5秒 を抜いても
      フル読み → 相関 +2.20秒（正解）
      切り貼り → 相関 +398.40秒（でたらめ）

音は「抜けている」ように見えるので気づきにくい。
**中身が正しいかは相関で確かめること。**

### 1カメだけの場面が混ざる

すべてのクリップにペアがあるとは限らない。`実技2.mov` はどれとも
重ならなかった（ユーザー確認済み・1カメで撮った場面）。
**重ならないクリップがあるのは正常**。異常として扱わないこと。

### 元の調査（SemiCut 側）

送り方（USB / Quick Share / LocalSend）による更新日時の違いは
`C:\dev\toolbox\SemiCut\引継ぎノート.md` の
「素材の実測値（もう一度 ffprobe しなくていいように）」にある。
**SemiCut のファイルは触らないこと**（読むだけ）。

## いまの状態（2026-09-19 時点）

**実素材6本で動いている。v11 公開済み。中断した作業は無い。**

    https://nknknmp.github.io/synccheck/

`Downloads\９１６烏口突起\` の6本での結果（所要4秒）:

    k1.mov   00:00:00:00   基準
    k1.mp4   00:01:08:06   音で測定（窓 4/5）
    k2.mp4   00:06:58:04   音で測定（窓 7/7）
    k3.mov   00:14:07:13   単独
    k3.mp4   00:14:17:12   音で測定（窓 2/2）
    k2.mov   00:17:09:06   単独

音だけで事前に求めた値（+68.19 / +418.14 / +9.95秒）と照合して
**3組とも誤差0フレーム**。ユーザー確認済み。

### 作りの要点（変えるときはここを壊さないこと）

1. **音で総当たりしてから並べる**
   撮影時刻で組を決めない。名前でも決めない（名前と中身が
   食い違うことがある）。全組み合わせを音で照合する。

2. **重なりの判定はスコアではなく「窓の一致」**
   相関スコアの大小では線を引けない（重ならない組でも0.23、
   重なる組が0.33のことがある）。候補位置で窓ごとに測り直して
   全窓が揃うかを見る（`sync.js` の `verifyOverlap`）。

3. **隣接はフレーム単位で**
   次の開始 = 前の開始フレーム + floor(長さ × fps)。
   +1 は不要（0始まり）。長さは切り捨て。秒のまま足すと
   丸めで1〜2フレームずれる。

4. **重ならないクリップがあるのは正常**
   1カメだけの場面が混ざる。異常として扱わない。

## ★ 次にやること

### 1. iPad 実機で確認（未確認）

PC の Chrome でしか確かめていない。v11 を開いて、実素材で
同じ結果が出るか見る。見出しの横が **v11** であることを確認。

### 2. 画面を白くする（未着手・依頼あり）

「画面が黒いのも嫌だ、白くして」という要望が保留のまま。
`index.html` の `:root` のカラー変数を差し替えれば済むはず。

### 3. JSON の書き出し（外したまま）

並べ方を変えたときに外した。旧形式（組ごと）が前提だったため。
必要になったら新しい形（1本の軸）で作り直す。

※ **fcpxml は 2026-09-20 の `a63dfee` で復活済み**（1本の軸の形で
   作り直した、既定オフのオプション）。この見出しが「fcpxml も
   外したまま」と読めたので直した（2026-09-25）。
   Resolve / FCP で開けるかは引き続き未確認。

## 触る前に動かすもの

    python serve.py
    → http://localhost:8770/test/e2e_check.html を開く

画面を通して動くかを見る。**Python のテストが全部通っていても、
画面が動かないことがある**（2026-09-19 に setBar() を消して
「押しても無反応」になった。ユニットテストは全通だった）。

---

# ▶ 次に開いたら、まずこれ（2026-09-19 時点）

## このリポジトリについて

**ここは `nknknmp/synccheck`（public）。`dev-toolbox` とは別のリポジトリ。**

出先で PC 無しに使うのが目的なので、GitHub Pages で公開している。
`dev-toolbox` は private のままにしたいが、private の Pages は有料なので
SyncCheck だけを `git subtree split` で切り出した（履歴は保っている）。

  公開先     : https://nknknmp.github.io/synccheck/
  リポジトリ : https://github.com/nknknmp/synccheck

main に push すると 1〜2分で反映される。

`C:\dev\toolbox\SyncCheck` にも同じものがあるが、**あちらは
dev-toolbox の中の古い方**。触るならこちらを直して push すること。
（両方に同じものが残っている状態。統合は下記「保留」参照）

## 状態

**出先で使える状態になった。オフラインでも起動する。**

測れること自体は実ファイルで確認済み（27件+42件のテスト通過）。
中断した作業は無い。

## ★ 次にやること（この順で）

### 1. iPad 実機で確認する（未確認・いちばん大事）

PC の Chrome では確認済みだが、**iPad では一度も動かしていない**。

  1. 家の Wi-Fi で https://nknknmp.github.io/synccheck/ を開く
  2. 1〜2分待つ（裏で31MB を取り込む）
  3. 共有 →「ホーム画面に追加」
  4. **機内モードにして**、アイコンから開く ← ここが本番
  5. 実際に2カメ素材を測ってみる

見るところ:
- 機内モードで起動するか（Service Worker が効いているか）
- 「写真」アプリの動画をファイル選択で渡せるか（iPadOS の挙動が未知）
- 長い動画でメモリ切れにならないか
  （Safari はタブごとの上限が PC より厳しい。1時間素材が通るか）

落ちたときの手当て:
- `src/audio.js` の `setLogging(true)` で ffmpeg の生ログを出す
- 単スレッド版なので `SharedArrayBuffer` は要らない。
  HTTPS でないことが原因になる処理は無い（Pages は HTTPS）

### 2. fcpxml が Resolve で開けるか試す（未確認・元からの宿題）

## 保留になっている判断

### dev-toolbox 側の SyncCheck をどうするか

今、同じコードが2か所にある:

  - `nknknmp/synccheck`（public）      ← これから直すのはこっち
  - `dev-toolbox` の `SyncCheck/`（private） ← 切り出し元。古くなる

選択肢:

  A. dev-toolbox から SyncCheck/ を消す
     - 二重管理が消える。いちばん素直
     - ただし toolbox 一式を clone しただけでは SyncCheck が付いてこない

  B. dev-toolbox 側に「移動しました」とだけ書いた README を残す
     - 次に開いた人が迷わない。おすすめ

  C. subtree で両方に同期し続ける
     - 手間が増える割に得が少ない

まだ決めていないので、**dev-toolbox 側はそのまま残してある**。

### 通信量のこと

初回の31MB を出先のモバイル回線で踏むと痛い。
今は「家の Wi-Fi で一度開いておく」と案内しているだけで、
アプリ側は何も言わない。

必要になったら、初回だけ「Wi-Fi で開くことを勧める」表示を足す。
実機で使ってみて、気にならなければ不要。

---

# ▶ 前回まで（2026-09-17 時点）

## 状態

**作れた。測れることは実ファイルで確認済み。中断した作業は無い。**
コミット `2608142` で push 済み。

## すぐ動かす

    cd C:\dev\toolbox\SyncCheck
    python serve.py

ブラウザが開く（http://localhost:8770/）。
A側・B側に動画を放り込んで「ズレを測る」を押すだけ。

※ `index.html` をダブルクリックしても**動かない**。
  `file://` ではブラウザが `import` 文を拒否するため。

## ★ 次にやること（この順で）

### 1. fcpxml が Resolve で開けるか試す（いちばん大事・未確認）

このPCに編集ソフトが無いので確かめられていない。

やること:
1. `python serve.py` で起動
2. 適当な2カメ素材を測る
3. 「fcpxml で保存」を押す
4. **Resolve で開いて、2本が同期して並んでいるか見る**

確認できているのは以下だけ:
- XML として整形式（ブラウザの DOMParser で検証済み）
- 時間がフレーム単位の分数になっている（1フレームずれ防止）
- クリップの位置が負にならない（負だと編集ソフトが読めない）

開けなかったときの手当て:
- 画面の「fcpxml のフレームレート」を素材と同じ値に変える
- それでもだめなら `src/export.js` の `buildFCPXML` を直す。
  SemiCut の `src/fcpxml.js` が実際に動いている実装なので、
  そちらの `<asset>` `<asset-clip>` の書き方に寄せるのが早い
- **急ぐなら「CSV で保存」で秒数を見て手で合わせる**（確実）

素材の場所はファイル名だけ書いてある（ブラウザは絶対パスを
知れないため）。開いたあとに素材を指定し直す必要がある。

### 2. 本番素材（数十分〜1時間）で試す（未確認）

テストは70秒の合成素材しか通していない。
ffmpeg.wasm はメモリ上限が厳しいので、長い素材で落ちる可能性がある。

落ちたら:
- 画面の「測る長さ」を 60秒 に下げる
- それでも落ちるなら `src/audio.js` の `extractSyncPCM` を
  「必要な区間だけ抜く」形に直す（いまもそうしているが、
  `writeFile` でファイル全体を MEMFS に入れているのが重い）

### 3. GitHub Pages を有効にするか決める（保留）

`vendor/`（31MB）をリポジトリに入れたので配信はできる状態。
ただし dev-toolbox は **Private** なので、Pages を使うには
公開するか有料プランが必要。

iPad で使いたいなら決める必要がある。
当面はこのPCで `python serve.py` で使える。

有効にするなら:
Settings → Pages → Source を main / (root) にして
`https://nknknmp.github.io/dev-toolbox/SyncCheck/` を開く

---

## 検算のしかた

    python test\sync_test.py        同期の計算 27件
    python test\fcpxml_test.py      fcpxml の書き出し 42件

ブラウザでの検算（`python serve.py` を先に動かす）:

| URL | 何を見るか |
|---|---|
| `/test/parse_check.html` | 各モジュールが読めるか・export 漏れ |
| `/test/wasm_check.html` | **ffmpeg.wasm で実ファイルから測れるか** |
| `/test/ui_demo.html` | 結果画面の見た目（3組で自動測定） |

`test/fixtures/` の素材は**正解が +3.130秒**。
ネイティブ ffmpeg で5通りの測り方をして全部一致した値。
ブラウザでも +3.130秒（差 0.000秒）が出ることを確認済み。

※ **Node.js はこのPCに入っていない。**
  `test/sync.test.mjs` は残してあるが動かせない。
  同じ内容を写した `test\sync_test.py` を使うこと。

---

## 触るときの注意（実測で分かったこと。消さないこと）

`src/sync.js` と `src/timeline.js` のコメントに詳しく書いてある。
要点だけ:

### 符号は紙の上で決めない

    D = LS - PS - lag   ← 正しい
    D = LS - PS + lag   ← 間違い

移植元（SemiCut）でここを間違えて、正しい部品まで疑って半日つぶした。
疑ったら**測定点を変えて同じ答えになるか**確かめること。
符号が逆だと、測定点をずらしたとき答えが2倍の速さで動く。

### スコアの高さは当てにならない。多数決で決める

セミナー音声は同じ部屋・同じ声で似た音が続くので、
**誤った位置のほうが高スコアになることが実際にある**。

このアプリのテストでも起きた:

    0秒目 → +3.130秒（スコア 1.177）
   20秒目 → +9.830秒（スコア 1.296）← 誤り。なのに最高スコア
   40秒目 → +3.130秒（スコア 1.180）

多数決にしているので 2対1 で正しい +3.13 を採用した。
スコアで選ぶ実装なら間違えていた。

### 重なりが短い lag は見ない（移植元には無かった修正）

`MIN_OVERLAP_RATIO = 0.5`。重なり長で割って正規化しても、
重なりが数秒まで減ると偶然そこだけ形が合って高スコアが出る。

20秒の窓に探索幅15秒を与えたとき:

    正解  +3.13秒（重なり17秒）
    誤答 -13.59秒（重なり 6秒）← こちらが高かった

「窓の半分以上は重なる lag だけ見る」条件で直した。
移植元は窓を30秒以上に保っていたので出ていなかった問題。

あわせて `src/measure.js` で、**全体で1回測ってから
その答え±5秒の範囲で窓ごとに測る**ようにしている
（窓に対して広すぎる探索幅を与えないため）。

### concat してから -ss してはいけない

移植元で、複数本をつないでから頭出しすると
4.069秒を頼んで 5.08秒 から返ってきた（1.01秒 late）。

**SyncCheck は必ず1本ずつ抜く。つなぐ処理を持たない**
（組ごとに測るので、つなぐ必要がそもそも無い）。
このアプリでは頭出しのズレ 0.000秒 を確認済み。

### 更新日時の意味は取り込み経路で変わる（素材23本の実測）

    mp4（スマホ）          更新日時 = 録画終了
    mov（iPad/LocalSend）  更新日時 = 録画終了
    MOV（iPad/USB）        更新日時 = 録画開始  ← これだけ違う

同じ iPad の同じ録画でも、取り込み経路で意味が変わる。
だから `resolveRecordingTimes` は両方の可能性を試して、
「2台のカメラは同じ時間帯を撮っている」という事実で決めている。

---

## 2026-09-17（新規作成）

### 作った理由

依頼: 「動画を同期する機能だけウェブアプリにしたい。
アップロードは避けたい」「すでにあるアプリとは別に作ってほしい」

アップロードは不要にできた。測るのに必要なのは
**8kHz モノラルの音量の線だけ**で、映像は1フレームも要らない
（ffmpeg に `-vn` を渡す）。だからブラウザの中で完結する。

### 主目的は「途中で切れて再開した素材」

ユーザーの言葉:
> 六カメというわけじゃなくて、二カメなんだけど、
> 途中切れてまた再開する感じがよくある

2カメでも A側3本・B側3本のようになり、**再開するたびに
ズレ方が変わる**。1組目の値を他の組に当てはめると全部ずれる。

そのため撮影時刻から重なる組を自動で見つけ、
**組ごとに独立して測る**設計にした。
組ごとのズレが1秒以上違えば警告を出す。

### ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | 画面 |
| `src/sync.js` | 相互相関・多数決（SemiCut から移植） |
| `src/timeline.js` | 複数本を時間軸に載せて組を見つける |
| `src/audio.js` | ffmpeg.wasm で音声を抜く |
| `src/measure.js` | 組ごとに測る |
| `src/export.js` | テキスト / CSV / JSON / fcpxml |
| `serve.py` | ローカル用の小さなサーバー |
| `vendor/` | ffmpeg.wasm 一式（約31MB・Git に入れている） |

### SemiCut とは別ファイルにしてある

同期の計算は移植したが、ファイルは独立している。
**直しても SemiCut に影響しない**（逆も同じ）。

移植元で直した内容をこちらに反映したいときは、
`SemiCut/src/core.js` の `crossCorrelate` / `verifySync` /
`resolveRecordingTimes` / `placeOnTimeline` を見比べること。

### vendor/ を Git に入れた（SemiCut とは方針が違う）

GitHub Pages で配信するにはリポジトリ内にファイルが必要なため。
SemiCut は `.gitignore` で除外しているが、こちらは入れている。

約31MB。SemiCut の37MBから、使わない日本語フォント（5.5MB）を除いた。
バイナリは一度入れたら更新されないので、リポジトリが膨らみ続けることはない。

親の `toolbox/.gitignore` が `*.mp4` を除外しているので、
`SyncCheck/.gitignore` でテスト素材だけ打ち消している（合計1.6MB）。
